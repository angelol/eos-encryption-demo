# eos-encryption-demo

## Build eosjs-ecc
```
cd eosjs-ecc
```
### Master doesn't seem to be working at the moment, so we're checking out latest release
```
git checkout v3.0.2
npm install
npm run build
```
